libclang.so compatible with Ubuntu 13.04 is included in the package.
All other versions and distros will need to compile a compatible version.

To compile libclang.so:
---------------------------
run compileclang.py
